<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="inviteListItemName">
        <item name="android:textColor">#5A5A5C</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="inviteListItemDate">
        <item name="android:textColor">#212c3b</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="inviteListItemHints">
        <item name="android:textColor">#5A5A5C</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="inviteListItemInviteId">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!--<style name="centroidButton">-->
    <!--<item name="android:textColor">@color/colorAccent</item>-->
    <!--<item name="android:background">@color/colorPrimaryDark</item>-->
    <!--<item name="android:typeface">monospace</item>-->
    <!--<item name="android:borderlessButtonStyle">@color/colorAccent</item>-->
    <!--</style>-->
    <!-- Application theme. -->
    <!-- All customizations that are NOT specific to a particular API-level can go here. -->

</resources>
